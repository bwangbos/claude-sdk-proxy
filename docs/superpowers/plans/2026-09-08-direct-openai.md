# Direct ChatGPT subscription backend implementation

## Global constraints

Implement the user-approved direct Pi-compatible subscription backend, not Codex app-server. Keep existing Claude defaults and behavior unchanged. Initial OpenAI models: `gpt-6-astra`, `gpt-5.6-sol`. Fixed HTTPS subscription endpoint; HTTP/SSE only; no public API billing fallback, model fallback, cookies, alternate accounts, runtime forks, or reading Pi/Codex credentials. Caller-owned tools only; no added persona or automatic execution. No merge, push, service activation, Pi configuration changes, repository rename, native /v1/responses, WebSockets, or additional models. Live tests require explicit proxy-owned login. Use TDD and retain sanitized verification evidence. Preserve unrelated user material.

## Task 1: Proxy-owned OAuth and credential lifecycle

Implement authentication modules and deterministic tests, with an explicit httpx production dependency and updated lockfile. Do not modify app routing yet. Public interface should expose an async credential manager supplying access token/account ID, refresh-once support, and safe status; CLI wiring is Task 3. New files should live under src/claude_sdk_proxy/openai_subscription/ with focused auth/storage modules.

Inspect installed Pi 0.85.1 source (under /Users/bwang/.nvm/versions/node/v24.15.0/lib/node_modules/@earendil-works/pi-coding-agent/dist/bundle/chunks/openai-codex.js) and its license, never credential files. Record exact reference version, OAuth URLs, public client ID, redirect URI and scope with attribution in docs/research/2026-09-08-openai-reference.md and retain applicable MIT notice. Do not impersonate Pi.

Implement browser OAuth/PKCE with cryptographically random state, constant-time state verification, loopback-only callback, bounded wait, error/cancellation cleanup and explicit user-triggered login. Bind before opening browser; occupied port fails safely. Validate token exchange and account identity; never log tokens, auth codes, verifier, raw responses, or credential repr. Fixed official auth endpoints. Runtime login must never use API-key billing or existing Pi/Codex credential files.

Store credentials solely under ~/.config/claude-sdk-proxy/ (test-injectable temporary directory). Owner-only directory/files, reject unsafe symlink/nonregular storage, atomic writes and refresh locking across concurrent tasks/processes, bounded async-friendly lock waits. Refresh double-check after lock, avoid overwriting newer login/logout, persist rotated tokens safely. Logout clears only proxy-owned OpenAI credentials. Missing/expired/revoked state produces actionable sanitized authentication error. Status must not expose secrets. Injectable httpx transport for local mocks is fine, but no production configurable inference/auth endpoint.

Tests: PKCE/state missing/mismatch/replay, callback paths/cleanup, token validation, permissions/atomic persistence, concurrent refresh (including separate store instances), revocation, logout race and redaction. All tests synthetic/no real login. Run focused tests, ruff, mypy and full non-live suite once before commit. Baseline known failure: tests/reset/test_domain.py::test_text_request_requires_alternating_messages_ending_in_user (2007 other tests pass). Do not alter that test or unrelated Claude code. Report RED/GREEN evidence and interfaces for next task.

## Task 2: Stateless subscription transport, translation and replay

Build OpenAI backend modules independent of Claude SessionRegistry, using Task 1 credentials. Send caller system instructions, messages/function tools directly, store:false, streaming upstream for both downstream modes, no default helpful/coding prompt or built-ins. Use fixed https://chatgpt.com/backend-api/codex/responses and honest proxy user-agent. Model-specific effort validation for the two approved models must be based on inspected Pi reference; unsupported fields fail before HTTP. Support text, images, image tool results, calls/results, summaries and encrypted reasoning without interpreting ciphertext. Preserve native call IDs and assistant item metadata.

Expose a provider-specific turn lease conforming to existing TurnLeaseProtocol (stream/abort/response_headers) for Task 3. Make minimal backwards-compatible domain extensions only as required for truthful model identity, safe provider error categories, refusal and usage. OpenAI input includes cached input; output includes reasoning. Never double count. Upstream reported model is evidence; requested name alone is not verified identity. Do not change Claude computations.

Replay: versioned OpenAI-specific opaque signature envelope for Anthropic thinking blocks, distinct from Claude signatures. Validate envelope shape, scope/account/model and bounds. Chat clients use bounded in-memory replay cache with --max-sessions entries and 64 MiB aggregate limit. Only exact unambiguous account/model/transcript matches reuse opaque state; missing/evicted/ambiguous/incompatible state degrades to visible history. Compaction replaces transcript, never recover dropped history. Reject unmatched results/incomplete tool boundaries before network. Shared prefixes must not serialize or collide; no parked tools or runtime subprocess.

SSE parser handles split UTF-8/frames, comments, fragmented arguments, multiple calls, repeated rounds, refusals, completed/incomplete/error events and missing completion. Safe errors only; diagnostics contain provider/requested/reported model/upstream request ID/status/category, never bodies or ciphertext. At most one auth refresh and one transient retry before content; no restart after emission. Cancel HTTP on timeout/disconnect, close pools at shutdown. Strictly bound buffers and ensure incomplete results never poison cache.

Tests with captured mock outbound bodies prove caller-only prompt/tools and headers. Cover all translation/replay/stream/lifecycle cases above and truthful accounting. Run focused tests/static checks/full non-live suite once. No live calls. Report interfaces to Task 3.

## Task 3: Existing HTTP dialects and CLI integration

Wire repeatable --model routing and /v1/models ownership for two exact OpenAI IDs; existing unknown/Claude aliases retain current behavior and default startup remains Claude-only. Add login openai/logout openai/auth-status openai commands without breaking existing CLI flags. Server never opens browser. Use proxy credential manager and shared HTTP pool, close during lifespan. OpenAI route bypasses Claude session machinery entirely, reuses downstream renderers only where semantic match. Explicit automatic fallback for OpenAI rejected; server Claude fallback defaults must not silently invoke fallback. Preserve all Claude fallback behavior.

Support both endpoints, stream/JSON: text, reasoning summaries/envelopes, function calls and results, images, refusal/completion reasons, truthful usage (cached/reasoning detail), actual model only when observed. Handle late model evidence honestly without false actual-model headers. Preserve request IDs and safe log diagnostics. Validate unsupported model capabilities and malformed boundaries before upstream. Missing cache after process restart allows full transcript continuation.

Add integration tests across both dialects covering selection/defaults/auth commands, payload capture, frames/tool argument fragmentation, parallel calls/repeated rounds, images/effort/signatures/refusals/errors/accounting, cancellation, shared prefixes, replay eviction/restart/model-account changes/compaction. Existing Claude suite remains unchanged. Full non-live verification and static checks; record baseline failure separately. No live service changes.

## Task 4: Documentation, opt-in live evidence and final verification

Document login/model configuration, endpoint dependency and unofficial authorization status without endorsement claims, included subscription limits and possible credit overage, no API billing fallback, replay limitations and reasoning fidelity loss, models/efforts, images, tool boundaries, usage and diagnostics. Add small bounded opt-in live battery for Astra and Sol: tool rounds, images, effort, compaction, cancellation and accounting, stock compatible harness without implementation modification. Never access Pi/Codex credentials. Run only after user completes new proxy login; otherwise explicitly report unverified live gates and concrete reproduction steps. Do not enable production backend or change Pi settings.

Run existing Claude compatibility suite unchanged, all new deterministic tests and lint/type checks. Finish with independent review and evidence, not automatic merge/push. Retain limitations honestly; no unmeasured performance claims.
